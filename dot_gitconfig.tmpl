[user]
  name = Jurre de Jongh
  email = ik@jur.re
  signingkey = {{ (onepasswordDetailsFields "vatdg4nytmu7puy6rmnj7nuaxq").public_key.value }}

[includeIf "gitdir:{{ .chezmoi.homeDir }}/Topicus/"]
  path = {{ .chezmoi.homeDir }}/Topicus/.gitconfig

[gpg]
  format = ssh

[gpg "ssh"]
  program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"

[commit]
  gpgsign = true

[core]
  excludesfile = {{ .chezmoi.homeDir }}/.gitignore_global
  autocrlf = input

[pull]
  rebase = false

[push]
  default = simple
  autoSetupRemote = true
